[phases.setup]
nixPkgs = ["nodejs-18_x"]

[variables]
CYPRESS_INSTALL_BINARY = "0"

[phases.install]
cmds = ["npm install --legacy-peer-deps"]

[phases.build]
cmds = ["npx prisma generate", "npm run build"]

[start]
cmd = "npm run start"