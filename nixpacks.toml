[phases.setup]
nixPkgs = ["nodejs-18_x"]

[variables]
CYPRESS_INSTALL_BINARY = "0"
NODE_ENV = "production"

[phases.install]
cmds = ["npm ci --omit=dev"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm run start"