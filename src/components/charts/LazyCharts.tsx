/**
 * LazyCharts - Centralized chart component exports
 * Provides lazy loading and consistent chart components across the application
 */

// Re-export all recharts components used in the application
export {
  LineChart,
  Line,
  AreaChart,
  Area,
  BarChart,
  Bar,
  PieChart,
  Pie,
  Cell,
  XAxis,
  YAxis,
  CartesianGrid,
  Tooltip,
  Legend,
  ResponsiveContainer
} from 'recharts';